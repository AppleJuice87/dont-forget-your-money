<resources>
    <style name="Custom_SideSheetDialog" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="android:windowBackground">@android:color/white</item> <!-- 배경색 설정 -->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.5</item>
        <item name="android:windowEnterAnimation">@anim/slide_in_right</item> <!-- 애니메이션 추가 -->
        <item name="android:windowExitAnimation">@anim/slide_out_left</item> <!-- 애니메이션 추가 -->
    </style>
</resources>
